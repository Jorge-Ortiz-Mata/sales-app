<div class="flex flex-col h-full w-full">
  <div class="flex flex-col overflow-y-auto p-5 gap-5">
    <div class="flex justify-between items-center">
      <h1 class="font-bold text-2xl text-sidebarTextColor">Editar articulo</h1>
    </div>

    <div class="border rounded shadow p-5">
      <%= render "form", { article: @article, btn_title: 'Editar articulo', cancel_path: article_path(@article) } %>
    </div>
  </div>
</div>
