<%= turbo_frame_tag 'admin_edit_user' do %>
  <%= render 'shared/modal', { title: 'Editar usuario' } do %>
    <div class="flex items-start w-full" >
      <ul class="flex flex-col w-3/12">
        <li class="cursor-pointer py-2 hover:bg-slate-100 duration-300 font-semibold text-sidebarTextColor px-5 border-b border-slate-200">Cuenta</li>
        <li class="cursor-pointer py-2 hover:bg-slate-100 duration-300 font-semibold text-sidebarTextColor px-5">Información general</li>
      </ul>
      <div class="flex w-9/12 py-2 px-5 border-l border-slate-200">
        <%= render 'admin/users/forms/edit_account', { user: @admin_user } %>
      </div>
    </div>
  <% end %>
<% end %>
