<main class="flex w-full">
  <div class="flex flex-col w-1/2 justify-between">
    <header class="flex items-center gap-2 p-5">
      <i class="fa-solid fa-truck-fast text-4xl"></i>
      <h1 class="font-bold text-xl">Paquetería Martínez Nashville</h1>
    </header>

    <section class="flex flex-col items-center justify-center">
      <div class="shadow rounded-lg border p-10 flex flex-col gap-5">
        <div class="flex flex-col w-full gap-3">
          <h2 class="font-semibold text-4xl text-center">Registrarme</h2>
          <p class="font-medium text-sidebarTextColor text-center text-sm">Crea tu cuenta de manera gratuita de forma rápida</p>
        </div>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'flex flex-col w-full gap-5 items-center'}) do |f| %>
          <%#= render "devise/shared/error_messages", resource: resource %>

          <div class="flex flex-col w-full items-start">
            <%= f.label :email, 'Correo electrónico', class: 'font-bold mb-1' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input-tailwindcss font-semibold #{ error_input_border(resource.errors.include?(:email)) }" %>
            <% if resource.errors.include?(:email) %>
              <%= render 'shared/input_error_msg', { object: resource, attribute: :email } %>
            <% end %>
          </div>

          <div class="flex flex-col w-full items-start">
            <%= f.label :password, 'Contraseña', class: 'font-bold mb-1' %>
            <%= f.password_field :password, autocomplete: "new-password", class: "input-tailwindcss font-semibold #{ error_input_border(resource.errors.include?(:password)) }" %>
            <% if @minimum_password_length %>
              <span class="text-sm text-sidebarTextColor font-medium italic">(<%= @minimum_password_length %> caracteres como minimo)</span>
            <% end %>
            <% if resource.errors.include?(:password) %>
              <%= render 'shared/input_error_msg', { object: resource, attribute: :password } %>
            <% end %>
          </div>

          <div class="flex flex-col w-full items-start">
            <%= f.label :password_confirmation, 'Confirmar contraseña', class: 'font-bold mb-1' %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input-tailwindcss font-semibold #{ error_input_border(resource.errors.include?(:password)) }" %>
            <% if resource.errors.include?(:password_confirmation) %>
              <%= render 'shared/input_error_msg', { object: resource, attribute: :password_confirmation } %>
            <% end %>
          </div>

          <div class="flex flex-col w-full items-center mt-10">
            <%= f.submit "Crear cuenta", class: 'text-white py-3 rounded-lg shadow bg-black duration-300 hover:bg-gray-700 cursor-pointer font-semibold w-full text-center' %>
          </div>
        <% end %>
      </div>
    </section>

    <footer class="flex items-center justify-center gap-2 py-5">
      <p class="font-medium text-sidebarTextColor">Ya tienes una cuenta?</p>
      <%= link_to 'Iniciar sesión', new_user_session_path, class: 'font-semibold underline hover:text-cyan-700 duration-300' %>
      <i class="fa-solid fa-arrow-right-to-bracket"></i>
    </footer>
  </div>

  <div class="flex w-1/2 h-full sign-up-image"></div>
</main>
